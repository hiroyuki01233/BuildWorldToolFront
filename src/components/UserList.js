import React from 'react';
import { Outlet } from "react-router-dom";

const UserList = () => {
  return (
      <div>
        <h1>
            UserList
        </h1>
      </div>
  );
}

export default UserList;