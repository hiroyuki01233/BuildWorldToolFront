import React from 'react';
import { Outlet } from "react-router-dom";

const Character = () => {
  return (
      <div>
        <h1>
            Character
        </h1>
      </div>
  );
}

export default Character;