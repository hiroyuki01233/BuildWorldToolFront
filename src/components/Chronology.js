import React from 'react';
import { Outlet } from "react-router-dom";

const Chronology = () => {
  return (
      <div>
        <h1>
            Chronology
        </h1>
      </div>
  );
}

export default Chronology;